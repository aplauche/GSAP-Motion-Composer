{
  "name": "gsap-motion-composer",
  "version": "0.0.2",
  "description": "This library serves as a wrapper for GSAP and ScrollTrigger to make some of the most common setups faster and easier to execute.",
  "scripts": {
    "dev": "rollup -c && vite",
    "build": "rollup -c && vite build",
    "preview": "vite preview",
    "dist": "rollup -c"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "rollup": "^4.40.2",
    "sass": "^1.89.0",
    "vite": "^6.0.3"
  },
  "dependencies": {
    "gsap": "^3.13.0"
  }
}
